<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Minimum password length -->
    <integer android:name="min_password_length">8</integer>

    <!-- Minimum number of letters -->
    <integer android:name="min_letters">1</integer>

    <!-- Minimum number of numeric characters -->
    <integer android:name="min_numeric">1</integer>

    <!-- Minimum number of special characters -->
    <integer android:name="min_symbols">1</integer>

    <!-- Minimum number of non-letter characters -->
    <integer android:name="min_nonletter">1</integer>

    <!-- Maximum password length -->
    <integer android:name="max_password_length">16</integer>
</resources>

